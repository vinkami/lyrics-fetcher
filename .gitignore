# Large transient/generated artifacts (not source)
_sep_out/
_lrc_re-ocr/
_backups/

# secrets + machine-local config — never commit
.env
.env.local
config.toml

# Python
__pycache__/
*.pyc
vendor/

# local working artifacts (album outputs, probes)
/out/
/poc/out/*
!/poc/out/stablets_results.json
